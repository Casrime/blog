{% extends 'layout.html.twig' %}

{% block layout %}
    <h2>Edit article {{ article.title }} with slug : {{ article.slug }}</h2>
    <div class="my-5">
        {{ include('_inc/_form.html.twig', {
            btn_class: 'btn btn-warning',
            icon: 'fa fa-check',
            text: 'Mettre à jour'
        }) }}

        {{ include('_inc/_link.html.twig', {
            btn_class: 'btn btn-danger',
            icon: 'fa fa-trash',
            path: path('delete_article', {
                slug: article.slug
            }),
            text: 'Supprimer'
        }) }}
    </div>
{% endblock %}
